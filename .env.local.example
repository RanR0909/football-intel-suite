# ================================================================
# AI / 抓取密钥 — 模板文件
# 使用：cp .env.local.example .env.local 并填入真实值
# 注意：.env.local 已在 .gitignore，永不提交
# ================================================================

# Claude API（默认中转：flashapi.top）
CLAUDE_API_KEY=

# Anthropic 官方端点（备用 / fallback；可与上方共存）
ANTHROPIC_API_KEY=

# X (Twitter) — 走第三方 fapi.uk / utools 服务（取代官方 X API v2）
# ⚠️  风险提示：该服务用你的 Twitter cookie 模拟登录抓取，违反 X ToS，存在封号风险
# ⚠️  强烈建议用「一次性小号」的 cookie，不要用主号 / 工作号
# ⚠️  cookie 通常 30 天内失效；失效时本爬虫会跳过并发飞书告警
#
# 获取步骤：
#   1. 浏览器登录 X（建议小号）
#   2. F12 DevTools → Application → Cookies → https://x.com
#   3. 找到 ``auth_token`` 行，复制 Value 列的值（一长串十六进制字符）
#   4. 填到下方
#
# 服务文档：https://utools.readme.io/reference/search-2
UTOOLS_AUTH_TOKEN=

# 旧的 X 官方 API Bearer Token（已弃用，配额不足支持 9 竞品扫描）
# X_BEARER_TOKEN=

# Google News 抓取（周更 RSS 模式，2026-04-30 起免 key，无需配置）
# 旧的 Google CSE API（GOOGLE_API_KEY / GOOGLE_CSE_ID）已弃用，改 RSS 后这两行不再需要
# 配置在 config/google_news.json（business_keywords + block_sources + 9 竞品 + 自家 site 排除）

# MySQL + Redis（2026-04-28 新加；本地 Docker 起步：docker compose up -d）
# MySQL: 长期事实数据（评论、广告、IAP、排名、社媒、sync_log）
# Redis: sync_state / retry_queue / sync_log:recent 镜像
# 任一未配置 → dao 层降级 JSON-only，dashboard 健康卡片显示"未配置"
MYSQL_DSN=mysql+pymysql://intelops:dev@localhost:3306/football_intel?charset=utf8mb4
REDIS_URL=redis://localhost:6379/0

# 飞书自定义机器人 Webhook — 可选，未配置时所有 Feishu 通知静默跳过
# 创建：飞书群 → 设置 → 群机器人 → 添加机器人 → 自定义机器人
# 安全建议选"关键词"，填 INTEL-OPS（或自定义，与下方 FEISHU_KEYWORD 一致）
# 触发场景：
#   - 每日 02:00 daily_sync 完成 → 结束卡片（绿/橙/红 按失败数）
#   - 周日 03:00 weekly_sync 完成 → 周更卡片
#   - Cookie 失效（fb_adlib / sensor_tower / appmagic）→ 即时红色卡片
#   - 每小时 retry-only 实际处理过任务 → 重试汇总
FEISHU_WEBHOOK_URL=
FEISHU_KEYWORD=INTEL-OPS

# 飞书卡片里"看预警 / 看同步日志"按钮的目标 URL（dashboard 部署后改成线上域名）
# 默认 http://localhost:5173 适配本地启动看板.command 流程
DASHBOARD_BASE_URL=http://localhost:5173
