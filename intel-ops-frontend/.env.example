# 复制为 .env.local 并按需调整
# VITE_API_BASE 控制 axios baseURL；不设时默认走 vite 的 /api proxy → http://localhost:8000

# 后端 API（生产环境填实际域名）
# VITE_API_BASE=https://api.intel-ops.example.com

# 开发期是否启用 MSW mock（不需要起后端）
# VITE_USE_MOCK=1
